services:
  bitcoin:
    restart: unless-stopped
    image: mrnaif/bitcart-btc:stable
    environment:
      BTC_NETWORK: ${BTC_NETWORK:-mainnet}
      BTC_LIGHTNING: ${BTC_LIGHTNING:-true}
    expose:
      - "5000"
    volumes:
      - "bitcoin_datadir:/home/electrum/.electrum"

  backend:
    depends_on:
      - bitcoin
    links:
      - bitcoin

volumes:
  bitcoin_datadir: